import { Button } from "./styles";

const story = {
  title: "Atom/Button",
  component: Button,
};

const Default = () => {
  return <Button> + Save</Button>;
};

export default story;
export { Default };
